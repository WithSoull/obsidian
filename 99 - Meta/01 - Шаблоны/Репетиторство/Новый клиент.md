<%*
/* Если файл имеет название "Untitled" — спросим имя и переименуем файл */
let title = tp.file.title;
if (!title || title.startsWith("Untitled")) {
  title = await tp.system.prompt("Имя файла (Имя ученика):", "");
  if (title && title.length) {
    await tp.file.rename(title);
  } else {
    title = tp.file.title || "Новый клиент";
  }
}

/* Запросы данных */
const studentPhone = await tp.system.prompt("Телефон ученика:", "");
const studentEmail = await tp.system.prompt("Email ученика:", "");
const studentTG = await tp.system.prompt("Telegram ученика (@username):", "");
const parentName = await tp.system.prompt("Имя родителя:", "");
const parentPhone = await tp.system.prompt("Телефон родителя:", "");
const parentEmail = await tp.system.prompt("Email родителя:", "");
const parentTG = await tp.system.prompt("Telegram родителя (@username):", "");
const level = await tp.system.prompt("Уровень (начальный / средний / высокий):", "");
const goals = await tp.system.prompt("Цели (кратко):", "");
const timePrefs = await tp.system.prompt("Предпочтения по времени:", "");
const tariff = await tp.system.prompt("Тариф (сумма за час / пакет):", "");

/* Собираем вывод в tR */
tR += `
**Дата добавления:** ${tp.date.now("YYYY-MM-DD HH:MM")}
**Кто добавил:** Лиза

## Контакты ученика
- **Телефон**: ${studentPhone}
- **Email**: ${studentEmail}
- **Telegram**: ${studentTG}

## Контакты родителя
- **Имя родителя:** ${parentName}
- **Телефон**: ${parentPhone}
- **Email**: ${parentEmail}
- **Telegram:** ${parentTG}

## Учебная информация
- **Уровень:** ${level}
- **Цели:** ${goals}
- **Предпочтения по времени:** ${timePrefs}

## Оплата
- **Тариф:** ${tariff}
`;
%>
