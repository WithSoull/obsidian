---
Сложность: Medium
Теги:
  - 2p
Понятность: 9/10
---
# Условие

![[99 - Meta/02 - Медиа/Pasted image 20240426014258.png]]\
# Объяснение

![[99 - Meta/02 - Медиа/Pasted image 20240426014619.png]]

# Решение

``` python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        left = 0
        right = len(nums) - 1

        while left < right:
            s = nums[left] + nums[right]
            if s > target:
                right -= 1
            elif s < target:
                left += 1
            else:
                return [left + 1, right + 1]
```

``` go 
func twoSum(numbers []int, target int) (answer []int) {
	left := 0
	right := len(numbers) - 1

	for left < right {
		s := numbers[left] + numbers[right]
		if s > target {
			right--
		} else if s < target {
			left++
		} else { // left + right == target
			answer = append(answer, left + 1, right + 1)
            return
		}
	}
	return
}

```