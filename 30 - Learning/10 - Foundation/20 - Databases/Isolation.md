# Основные проблемы, которые возникают

#### I. Dirty read (грязное чтение)
![[99 - Meta/02 - Медиа/Pasted image 20250217210333.png]]

 > Аномалия заключается в том, что данные который я прочитал, кто-то может откатить до моего коммита
#### II. Non Repeatable Read (Fuzzy Read, Неповторяющееся чтение)
 ![[99 - Meta/02 - Медиа/Pasted image 20250217210550.png]]

> Эта аномалия происходит тогда, когда данные которые я прочитал, кто-то может изменить до моего коммита
#### III. Phantom Read (Фантомное чтение)
![[99 - Meta/02 - Медиа/Pasted image 20250217213907.png]]

> Это аномалия происходит тогда, когда ряд данных, который я прочитал, кто-то может изменить до того, как я завершу свою транзакцию

# Уровни изоляции
#### I. Read UnComitted
> Уровень изоляции при котором, можно читать незакомиченные данные

Проблемы этого уровня изоляции
1) Dirty Read
2) Non-repeatable Read
3) Phantom Read

В общем все проблемы, которые встречаются с транзакциями присутствуют на этом уровне

#### II. Read Commited
> PostgreSQL, MS SQL, Oracle

> На этом уровне можно читать только те изменения, которые уже закомиченны

Этот уровень изоляции спасает нас только от *Dirty Read*

![[99 - Meta/02 - Медиа/Pasted image 20250217214723.png]]

Проблемы здесь не появляются потому что, незакомиченные данные наша транзакция "не видит"

> Почему *Non-repeatable Read* и *Phantom Read* все еще являются проблемами для этого уровня?

Проблема возникает потому, что данные которые мы прочитали, может успеть изменить другая транзакция и закоммитить эти изменения
##### Non-repeatable Read в Read Commited
![[99 - Meta/02 - Медиа/Pasted image 20250217214953.png]]
##### Phantom Read в Read Commited
![[99 - Meta/02 - Медиа/Pasted image 20250217215044.png]]

#### III. Repeatable Read
> MySQL, InnoDB (движок для хранения данных в MySQL) с уровнем изоляции  *Repeatable Read* даже фантомное чтение не страшно. Почему? Там есть штука, которая называется [[30 - Learning/10 - Foundation/20 - Databases/MVCC]] — о ней мы чуть позже поговорим.

> На этом уровне пока транзакция прочитана, никто параллельно не может изменять строки, которые эта транзакция прочитала
> 
> Данные, которые я прочитал, никто не изменит до окончания транзакции

Этот уровень изоляции спасает нас только от *Dirty Read* и *Non-repeatable Read*

> Почему?
> Потому что, в этих проблемах изменялись уже прочитанные нами строки, чего на данном уровне делать нельзя

Остается проблема *Phantom Read*, потому что прочитанные строки изменять нельзя, а вот другие можно. Иллюстрация ниже:

![[99 - Meta/02 - Медиа/Pasted image 20250217220053.png]]

#### IV. Serializable Read
> Запрещает вообще трогать таблицу, строки которой читаются в нашей транзакции

Тут никакие аномалии нам не страшны — ни грязное, ни неповторяемое, ни фантомное чтение. Но и производительность тут страдает, ведь транзакции не могут работать параллельно.

# Табличка со всеми проблемами и уровнями изоляции
![[99 - Meta/02 - Медиа/Pasted image 20250217220646.png]]

# Всего 3 аномалии?
Но также на уровнях изоляции READ UNCOMMITTED и READ COMMITTED могут ещё возникнуть такие аномалии, как:

- **Потерянное обновление (Lost Update)** — когда две транзакции одновременно читают и изменяют одни и те же данные, и при этом одно из изменений может потеряться.
    
- **Неупорядоченное чтение (Out‑of‑order Read**) — когда несколько чтений выполняются в произвольном порядке, что может привести к неправильным результатам в транзакциях.

# Что если?
#### Как можно переключаться между уровнями?
``` sql
	--MySQL
	--Будет действовать на 1 следующую транзакцию в текущей сессии
	SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
	
	--Будет действовать на все транзакции в текущей сессии
	SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;

	--PostgreSQL
	--Вариант включения уровня изоляции в PostgreSQL
	SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL REPEATABLE READ;
	
	--Будет действовать на все транзакции
	SET GLOBAL TRANSACTION ISOLATION LEVEL REPEATABLE READ;
```

### А если столкнуть две транзакции с разными уровнями изоляции?

Если параллельно работают несколько транзакций с разными уровнями изоляции, то могут возникать проблемы с видимостью данных и появляться наши любимые аномалии. Опять же, тут всё зависит от вашей СУБД, но вот несколько базовых понятий:

- Транзакция с высоким уровнем изоляции (например, SERIALIZABLE) блокирует ресурсы более строго, что может привести к блокировкам для других транзакций с более низким уровнем изоляции (например, READ UNCOMMITTED или READ COMMITTED);
    
- Если транзакция с более низким уровнем изоляции пытается получить доступ к данным, которые заблокированы более изолированной транзакцией, она будет ждать, пока та не завершится (или не будет отменена);
    
- Транзакция с низким уровнем изоляции (например, READ UNCOMMITTED) может видеть незавершенные изменения других транзакций и не блокирует их, что может привести к ситуациям, когда она читает данные, которые в итоге могут быть отменены в более изолированной транзакции.