## Состояние горутины
1) *Running* - выполняется
   Это значит что ядро выполняет инструкции из этого потока (по регистру IP - instruction pointer)
2)  *Runnable* - готова к выполнению
3) *Waiting* - остановлена и ожидает чего-то (например на примитиве синхронизации или ожидает syscall)
# Масштабирование
## N : 1 модель

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.05.15.png]]

Тут у нас есть очередь, внутри которой есть горутины, при этом внутри эти горутины выражены структурой с указателем на стек и на регистры, при этом регистры зачастую лежат на стеке.

## Как и когда будем вытеснять горутины?
Например, можно сделать кооперативную многозадачность, тогда копмилятор сам добавит в исходный код места для переключения контекста. 

## Как быть серверам с многоядерными процессорами?

>[!danger] В любой очереди дальше G - означает _runnable_ горутину
>Ибо незачем хранить _waiting_ горутину.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.13.51.png]]

В целом все тоже самое, только тут N планировщиков

>[!danger] Такая система не масштабируема
>
>![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.15.26.png]]
>
>Потому что захотев поставить например mutex, он будет пропускать только один поток, а остальные будут простаивать. И поэтому в этом _bottleneck_ образуется *contention*.
>
>![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.22.02.png]]
>
>Lock-free тоже будет тяжело машстабировать


![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.23.22.png]]

Делаем для каждого потока свою локальную очередь. Так синхронизация нам не нужна.

>[!info]  GMP Модель
>G - Goroutine (что исполняем)
>M - Machine (где исполняем)
>P  - Processor (права и ресурсы для исполнения)
>

``` go 
package main

import (
	"fmt"
	"runtime"
)

func main() {
	fmt.Println("GOMAXPROCS:", runtime.GOMAXPROCS(0))
	fmt.Println("CPU:", runtime.NumCPU())

	runtime.GOMAXPROCS(16)

	fmt.Println("GOMAXPROCS:", runtime.GOMAXPROCS(0))
	fmt.Println("CPU:", runtime.NumCPU())
}
```

``` output
GOMAXPROCS: 10
CPU: 10
GOMAXPROCS: 16
CPU: 10
```

GOMAXPROCS() - показывает сколько P (из GMP модели) у нас установлено.

>[!info] По умолчанию количество *Р* берется из значения переменной среды *GOMAXPROCS* и равно количеству логических ядеркомпьютера.

## В какую из очередей добавлять новую горутину?
![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.38.05.png]]

_Ответ:_ В ту же из которой она была запущенна

## Но что тогда делать если в очереди закончаться горутины?
![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.39.27.png]]

Потоки могут между собой делиться горутинами, но тут слишком много вопросов, когда делиться, кому и сколько, как часто, в общем тяжко.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.41.11.png]]

_Work stealing_ проще и эффективные. Когда потоку нечего исполнять, он может скоходить в другой поток и украсть у него горутину.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.42.35.png]]

Но тогда нужно навесить примитивы синхронизации, как вариант: _lock-free_

>[!question] Но у какой очереди красть?
>У рандомной

>[!question] Что делать, если в другой очереди тоже нет горутин?
>Попробуем украсть N раза
>

>[!question] Сколько красть из другой локальной очердь?
>Украдем ровно половину

# SYSCALLS

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.48.34.png]]

Ну когда горутине нужно обработать syscall, это дело блокирует и саму горину, и поток, в которой она исполняется, потому что этому потоку нужно свичнуться на ядро

#### HandOff

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.52.51.png]]

Перед тем как вызвать syscall поток отрубает связь с очередью, чтобы очередь не сидела без своего потока

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.54.15.png]]

И дабы не создавать каждый раз новый поток, можно завести _thread pool_

## Куда девать горутину после выполнения syscall-a?

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 09.57.21.png]]

Постараемся запихнуть ее туда же где она была, но если *P* будет недоступен, то она попадет в глобальную очередь, на которую навешан мьютекс.

## А что тогда делать если у нас в сервисе открыто 10к соединений, это че, 10к потоков будет если все они syscall будут обрабатывать?

А если стек потока равен 8мб, значит потребуется 80гб ОЗУ, ну это не дело.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 11.05.18.png]]

Когда идем прочить что-то из сокета, мы получаем ошибку/статус, на основе которой можно понять что там данных нет. Спустя время можно поретраить/полить этот сокет. Т. е. мы не блокируемся. Что бы поток не занимался этим постоянно есть мультиплексор, для линукса это epoll например. Мы туда отдаем наш [[30 - Learning/10 - Foundation/10 - CPU Arch/Resource Descriptors|файловый дескриптор (сокет в данном случае)]]. Мы переодически опрашиваем этот epoll, ждем сообщение на каком-то из дескрипторов из всего множества файловых дескриторов, которые можно отдать epoll-у. Нам все равно нужно полить тогда этот epoll. В го есть **sysmon**, который опрашивает epoll, конкретно этим занимается _netpoller_, который занимается системными вызовами в сети. При этом у нас там в epoll для каждого дескриптора стоит указатель на свою горутину (G).

# Очереди
![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 11.17.13.png]]

## Как быть с горутинами в глобальной очереди?
Они ведь по факту голодают там.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 11.17.58.png]]

Раз в 61 планирование, проверяется глобальная очередь. Из комментария, видно, что если поток не нашел себе горутину в локальной очереди, ни в глобальной, он идет полить epoll. А значит не только *sysmon* может полить epoll. 

## Как быть с кэшами, когда несколько горутин берут общую блокировку?
![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 11.23.11.png]]

Если очередь FIFO, то кэши горутины уже протухнут, а если LIFO, то с наебольшей вероятностью там будут свежие прогретые кэши. Но в LIFO очереди будут голодать левые горутины.

![[99 - Meta/02 - Медиа/Снимок экрана 2025-09-18 в 11.24.44.png]]

Поэтому в го используется симбиоз, двух очередей, но при этом LIFO часть состоит только из одного элемента.

## Как быть с циклами при общении горутин через несколько каналов?
Тогда получается две эти горутины, которые сейчас между собой общаются будут ставить друг друга в очередь LIFO куска.

Считаем время непрерывной работы и в случае превышения временного лимита, вытисняем из LIFO. За этим следит *SYSMON*

# Циклы
## Как быть с бесконечными циклами или CPU-BOUND задачами?

>[!danger] Асинхронная вытисняющая многозадачность (ASYNCHRONOUS PREEMPTION)
>Как только, обнаруживается горутина, работающая более 10мс, в поток передается сигнал для ее вытиснения (SIGURG). Этим также занимается _SYSMON_. Важно понимать, что переключение произойдет не сразу, потому что код может быть в unsafe месте, и garbage collector не может его отпустить. И когда там все доделается :) тогда и произойдет context switching. Поэтому она асинхронная.

``` go
package main

import (
	"log"
	"runtime"
)

func infiniteLoop(str string) {
	for {
		log.Println(str)
	}
}

func loop(str string) {
	for i := 0; i < 5; i++ {
		runtime.Gosched()
		log.Println(str)
	}
}

func main() {
	runtime.GOMAXPROCS(1)
	go infiniteLoop("infinite_loop")
	loop("loop")
}
```

``` output
2025/09/18 11:31:05 infinite_loop
... (таких строчек очень много)
2025/09/18 11:31:05 infinite_loop
2025/09/18 11:31:05 loop
```

`runtime.Goshed()` позволяет попросить планировщика переключиться на другую горутину. При этом текущая горутина ставиться в режим _runnable_

Ну вот смысл этого примера в том, что sysmon останавливает горутины, которые работают слишком долго 

## Как закрепить горутину за конкретным потоком?
``` go
runtime.LockOSThread()
runtime.UnlockOSThread()
```

Но это нужно в супер редких кейсах
## Что бы принудительно завершить горутину?
``` go
runtime.Goexit()
```

Такая возможность есть, но крайне редко стоит ей пользоваться. Отключать горутину в рандомный момент это безумие.

## Когда завершается выполнение программы на Go?
Когда завершится main-горутина. 

``` go
package main

import (
	"fmt"
	"runtime"
	"time"
)

func main() {
	go func() {
		fmt.Println("first")
		runtime.Goexit()
		fmt.Println("second")
	}()

	time.Sleep(3 * time.Second)
}
```

``` output
first
```

Ну до `second` не дошло потому что мы убили горутину до этого вывода, а значит, горутина остановиться на том месте где успела закончить(

``` go
package main

import (
	"fmt"
	"runtime"
	"time"
)

func main() {
	go func() {
		for {
			time.Sleep(time.Second)
			fmt.Println("tick")
		}
	}()

	time.Sleep(3 * time.Second)
	runtime.Goexit()
}
```

``` output
tick
tick
tick
tick
...
```

Мы убили main горутину, но при этом оставили жить другую, которая продолжает работать, магия :)